name: LeetCode sync

on:
  schedule:
    - cron: "17,47 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: leetcode-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out sync script
        uses: actions/checkout@v4
        with:
          path: script

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: script/package-lock.json

      - run: npm ci
        working-directory: script

      - name: Check out solutions repository
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOLUTIONS_REPO }}
          token: ${{ secrets.SOLUTIONS_REPO_TOKEN }}
          path: solutions

      - name: Configure commit author
        working-directory: solutions
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Sync accepted submissions
        id: initial-sync
        continue-on-error: true
        env:
          LEETCODE_SESSION: ${{ secrets.LEETCODE_SESSION }}
          LEETCODE_CSRF_TOKEN: ${{ secrets.LEETCODE_CSRF_TOKEN }}
        working-directory: solutions
        run: node ../script/sync.js

      - name: Fail non-authentication sync errors
        if: steps.initial-sync.outcome == 'failure' && steps.initial-sync.outputs.status != 'auth'
        run: exit 1

      - name: Install Chromium for reactive re-login
        if: steps.initial-sync.outputs.status == 'auth'
        run: npx playwright install --with-deps chromium
        working-directory: script

      - name: Refresh LeetCode session after authentication failure
        if: steps.initial-sync.outputs.status == 'auth'
        env:
          LEETCODE_USERNAME: ${{ secrets.LEETCODE_USERNAME }}
          LEETCODE_PASSWORD: ${{ secrets.LEETCODE_PASSWORD }}
          GH_SECRETS_TOKEN: ${{ secrets.GH_SECRETS_TOKEN }}
        run: node refresh-session.mjs
        working-directory: script

      - name: Retry sync once with the fresh session
        if: steps.initial-sync.outputs.status == 'auth'
        working-directory: solutions
        run: node ../script/sync.js
